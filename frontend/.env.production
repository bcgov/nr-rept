# Loaded by `vite build` (mode=production). Values here are baked into the
# built bundle at compile time as `import.meta.env.X`.
#
# Set the SPA's URL base + react-router basename + cookie path scope to '/'
# now that both Caddy and Spring serve at root.
VITE_BASE_PATH=/
