name: Merge

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - '.github/graphics/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions: {}

jobs:
  init:
    name: Initialize
    permissions:
      contents: read
      pull-requests: read
    outputs:
      pr: ${{ steps.pr.outputs.pr }}
    runs-on: ubuntu-24.04
    steps:
      # Find the PR number associated with this squash-merged commit on main,
      # so we can deploy the same image tag that the PR was running.
      - id: pr
        uses: bcgov/action-get-pr@3931962bf13b3f4702d0013ec7c836f752ff1a3b # v0.3.0

  deploy-test:
    name: TEST Deploys (${{ needs.init.outputs.pr }})
    needs: [init]
    # contents:read here so the nested reusable-deploy jobs can checkout.
    # Workflow-level `permissions: {}` above sets all scopes to none; the
    # called workflow can't grant itself more than the caller has.
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-deploy.yml
    secrets:
      oc_namespace: ${{ secrets.oc_namespace }}
      oc_token: ${{ secrets.oc_token }}
      database_user: ${{ secrets.database_user }}
      database_password: ${{ secrets.database_password }}
      database_host: ${{ secrets.database_host }}
      database_service_name: ${{ secrets.database_service_name }}
      keystore_secret: ${{ secrets.keystore_secret }}
      keycloak_sa_client_id: ${{ secrets.KEYCLOAK_SA_CLIENT_ID }}
      keycloak_sa_client_secret: ${{ secrets.KEYCLOAK_SA_CLIENT_SECRET }}
      user_lookup_base_url: ${{ secrets.USER_LOOKUP_BASE_URL }}
    with:
      environment: test
      target: test
      tag: ${{ needs.init.outputs.pr }}
      backend_replicas: "2"
      frontend_replicas: "2"

  tests:
    name: Tests
    # Set the repository variable SKIP_E2E to `true` to skip the browser suite.
    # Neither this job nor reusable-tests.yml declares an `environment:`, so the
    # value is read from repository (or org) variables — one switch covering both
    # this workflow and pr-open.yml. PROD still deploys; see deploy-prod below.
    if: vars.SKIP_E2E != 'true'
    needs: [deploy-test]
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-tests.yml
    secrets:
      e2e_idir_user: ${{ secrets.E2E_IDIR_USER }}
      e2e_idir_password: ${{ secrets.E2E_IDIR_PASSWORD }}
    with:
      target: test

  deploy-prod:
    name: PROD Deploys (${{ needs.init.outputs.pr }})
    # `always()` is required because a skipped dependency otherwise skips this
    # job and everything downstream of it. With it, each upstream result must be
    # checked explicitly:
    #   - tests SKIPPED is fine (that is the flag doing its job)
    #   - tests FAILED or CANCELLED still blocks PROD
    #   - deploy-test must be named and required, because if IT fails, `tests`
    #     is skipped too — and "skipped" alone would otherwise wave PROD through
    #     on top of a broken TEST deploy.
    needs: [tests, init, deploy-test]
    if: >-
      always()
      && needs.init.result == 'success'
      && needs['deploy-test'].result == 'success'
      && needs.tests.result != 'failure'
      && needs.tests.result != 'cancelled'
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-deploy.yml
    secrets:
      oc_namespace: ${{ secrets.oc_namespace }}
      oc_token: ${{ secrets.oc_token }}
      database_user: ${{ secrets.database_user }}
      database_password: ${{ secrets.database_password }}
      database_host: ${{ secrets.database_host }}
      database_service_name: ${{ secrets.database_service_name }}
      keystore_secret: ${{ secrets.keystore_secret }}
      keycloak_sa_client_id: ${{ secrets.KEYCLOAK_SA_CLIENT_ID }}
      keycloak_sa_client_secret: ${{ secrets.KEYCLOAK_SA_CLIENT_SECRET }}
      user_lookup_base_url: ${{ secrets.USER_LOOKUP_BASE_URL }}
    with:
      environment: prod
      target: prod
      tag: ${{ needs.init.outputs.pr }}
      backend_replicas: "3"
      frontend_replicas: "3"

  # Sync Sysdig email alerts for PROD. No-ops cleanly if SYSDIG_API_TOKEN is
  # unset — adoption is gradual. Alert templates live in monitoring/alerts/.
  monitor-prod:
    name: Sysdig Monitor (PROD)
    needs: [deploy-prod]
    runs-on: ubuntu-24.04
    environment: prod
    permissions:
      contents: read
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: bcgov/actions/sysdig-monitor@4ad61a784f1c17765b03d8d6de9737c1d3f4c0f2 # v0.5.0
        with:
          sysdig_api_token: ${{ secrets.SYSDIG_API_TOKEN }}
          oc_namespace: ${{ secrets.oc_namespace }}
          app: nr-rept

  # After PROD is up, alias the PR image as :prod in GHCR. Creates a durable
  # record of "this is the image running in production right now" and a
  # rollback target.
  promote:
    name: Promote Images
    needs: [deploy-prod, init]
    runs-on: ubuntu-slim
    permissions:
      packages: write
    strategy:
      matrix:
        package: [backend, frontend]
    timeout-minutes: 1
    steps:
      - uses: shrink/actions-docker-registry-tag@e6aaef25c595b6e0edd18bf4c7dbfea3abd43299 # v5
        with:
          registry: ghcr.io
          repository: ${{ github.repository }}/${{ matrix.package }}
          target: ${{ needs.init.outputs.pr }}
          tags: prod
